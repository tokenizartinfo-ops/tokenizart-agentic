# Schemas

Versioned JSON Schemas and contracts describe the stable public Tokenizart
client boundary. Runtime implementation remains hosted separately.

Consumers must reject responses that exceed the declared access level or omit
the required source, status and no-mutation fields.

